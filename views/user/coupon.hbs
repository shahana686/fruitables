<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

</head>
<h2 class="mb-4 text-center">Available Coupons</h2>

{{#if coupons.length}}
  <div class="row">
    {{#each coupons}}
      <div class="col-md-6 col-lg-4 mb-4">
        <div class="card border-success shadow">
          <div class="card-header bg-success text-white">
            <strong>Coupon Code: {{this.code}}</strong>
          </div>
          <div class="card-body">
            <h5 class="card-title">Discount: ₹{{this.discount}}</h5>
            <p class="card-text">Minimum Purchase: ₹{{this.minPurchase}}</p>
            <p class="card-text">Expiry Date: {{this.expiryDate}}</p>
            <form method="POST" action="/user/apply-coupon">
              <input type="hidden" name="code" value="{{this.code}}">
               <form id="applyCouponForm">
              {{!-- <button class="btn btn-primary mt-2" type="submit">Apply Coupon</button> --}}
            </form>
          </div>
        </div>
      </div>
    {{/each}}
  </div>
{{else}}
  <div class="alert alert-warning text-center mt-4">
    No coupons available for your cart total (₹{{subtotal}}). Add more items to unlock discounts.
  </div>
{{/if}}

<body>
</body>
</html>