
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Product List</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

  <div class="container py-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <h2>Product List</h2>
      <a href="/admin/product/add" class="btn btn-primary" enctype="multipart/form-data">➕ Add Product</a>
    </div>

    {{#if products.length}}
      <div class="table-responsive">
        <table class="table table-bordered table-hover align-middle">
          <thead class="table-dark">
            <tr>
              <th>Image</th>
              <th>Name</th>
              <th>Description</th>
              <th>category</th>
              <th>Price</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            {{#each products}}
              <tr>
                {{!-- <td><img src="{{this.image}}" alt="{{this.name}}" width="70"></td> --}}
                <td>
                      {{#each this.images}}
      <img src="{{this}}" alt="Product Image" width="100" />
    {{/each}}
                </td>
                <td>{{this.name}}</td>
                <td>{{this.description}}</td>
                 <td>{{this.category}}</td>
                <td>₹{{this.price}}</td>
                <td>
                  <a href="/admin/product/edit/{{this._id}}" class="btn btn-sm btn-warning">Edit</a>
                  <a href="/admin/product/delete/{{this._id}}" onclick="return confirm('Are you sure?')" class="btn btn-sm btn-danger">Delete</a>
                </td>
              </tr>
            {{/each}}
          </tbody>
        </table>
      </div>
    {{else}}
      <p>No products found.</p>
    {{/if}}
  </div>

</body>
</html>
